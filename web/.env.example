# 後端 API 基底 URL（dev proxy 會將 /api/* 轉發至此）
VITE_API_BASE_URL=http://localhost:5000

# 預設 API 路徑前綴
VITE_API_PREFIX=/api

# Azure AD / Entra ID 設定（issue #34 [2.1.2] 已啟用）
VITE_AZURE_CLIENT_ID=
VITE_AZURE_TENANT_ID=
VITE_AZURE_REDIRECT_URI=http://localhost:5173
VITE_AZURE_API_SCOPE=

# RBAC mock 模式（issue #8 [2.2.2] 加入）
VITE_USE_MOCK_RBAC=true

# 自訂欄位 mock 模式（issue #11 [3.1.2] 加入）
VITE_USE_MOCK_FIELDS=true

# 流程範本 mock 模式（issue #13 [3.2.2] 加入）
VITE_USE_MOCK_TEMPLATES=true

# 案件 mock 模式（issue #21 [5.5] 加入）
# true  → 攔截 /cases 請求，使用 mockCases.ts 裡的假資料
# false → 送到真實後端（要等 issue #9/#15/#17/#18/#20 落地才能使用）
VITE_USE_MOCK_CASES=true
